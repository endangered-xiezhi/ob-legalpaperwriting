services:
  - type: web
    name: ob-legalpaperwriting
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist/client
    envVars:
      - key: NODE_VERSION
        value: 22
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
